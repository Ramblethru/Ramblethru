<div class="pagename">
  <h1>Discover your destination</h1>
  <div class="location"> Location name </div>
  <div class='map'>
    <iframe width='100%' height='500px' frameBorder='0' src='https://a.tiles.mapbox.com/v4/daninyc.ka9776m1/attribution,zoompan,zoomwheel,geocoder,share.html?access_token=pk.eyJ1IjoiZGFuaW55YyIsImEiOiJsZGtFNGdnIn0.3NYblJVlOSsj0kll5c4aKA'></iframe>
  </div>
  <div class="notes"><h4>NOTES</h4></div>
  <div class="noteboxes">Create note partial here</div>
  <div class="foursq">
    <div class="title">Foursquare:</div><br>
     <% f = @foursquare_venue.each do |f| %>
        <li>
        <div class="title">
          <%= f["venue"]["name"] %>
        </div>
        </li>
          Address: <%= f["venue"]["location"]["address"] %><br>
        <% f["tips"] %>
        <% f["tips"].each do |g| %>
        <article><%= link_to g["text"], g["canonicalUrl"] %></article>
        <% end %>
    <% end %>
  </div>
  <br>
  <div class="yelp">
    <div class="title">Yelp:</div>
    <br>
    <% @yelp.businesses.each do |y| %>
      <li>
        <%= link_to y.name, y.url %>
        <%= link_to image_tag y.rating_img_url_small %>
      </li>
        <article><%= y.snippet_text %></article>
      <% end %>
  </div>
  <br>
  <div class="instagram">
    <div class="title">Instagram:</div>
    <br>
    <% i = @instagram_images.each do |i| %>
      <%= link_to image_tag i["images"]["thumbnail"]["url"] %>
    <% end %>
  </div>
  <br>
  <div class="reddit">
    <div class="title">Reddit:</div><br>
    <% @reddit_thread.each do |r| %>
      <li><%= link_to r["data"]["title"], r["data"]["url"] %></li>
    <% end %>
  </div>
  <div class="ramble">
  <%= simple_form_for Ramble.new do |f| %>
      <%= f.input :destination, label: false, placeholder: "Destination" %>
      <%= f.input :start_date, label: false, :class=>"tag-input", :placeholder=>"From" %>
      <%= f.input :end_date, label: false, :class=>"tag-input", :placeholder=>"To" %>
    <%= button_to 'Save this Ramble', { :controller => 'rambles', :action => 'create'} %>
  <% end %>
  <span>or</span>
  </div>
  <form class="search-form">
    <div class="location-search">
      <input type="text" placeholder="Search for more">
      <input type="submit" value="Search">
    </div>
  </form>
</div>
