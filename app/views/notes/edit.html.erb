<%= simple_form_for [@ramble, @note] do |f| %>
    <%= f.input :body, label: false, placeholder: "Your body text" %>
    <%= f.input :tag_list, label: false, :class=>"tag-input", :placeholder=>"Tags" %>
    <%= f.button :submit, "Edit note", :class => "radius" %>
<% end %>