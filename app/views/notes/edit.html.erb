<%= simple_form_for [note] do |f| %>
    <%= f.input :body, label: false, placeholder: "Note" %>
    <%= f.input :tag_list, label: false, :class=>"tag-input", placeholder: "Tag" %>
    <%= f.button :submit, "Add note", :class => "radius small" %>
<% end %>

