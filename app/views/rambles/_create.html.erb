<%= simple_form_for @ramble do |f| %>
	<h1 class="ramble-create">Create a Ramble</h1>
	<%= f.input :name, label:false, placeholder: "Give your ramble a name", id:'ramble_name' %>
	<%= f.input :destination, label: false, placeholder: "Destination (REQUIRED)", id:'ramble_destination' %>
	<%= f.text_field :start_date, label: false, placeholder: "Start Date", id: 'start_date' %>
	<%= f.text_field :end_date, label: false, placeholder: "End Date", id: 'end_date' %>
	<%= button_to 'Save this Ramble', { :controller => 'rambles', :action => 'create'}, id: 'ramble-button' %>
<% end %>
<script type="text/javascript">
  $(document).ready(function(){
  $('#ramble_name').val("title");
  });
  $( "#start_date" ).datepicker({
        showButtonPanel: true,
        showOtherMonths: true,
        selectOtherMonths: true,
        defaultDate: "+1w",
        showAnim:"slideDown",
        numberOfMonths: 2,
        dateFormat:'yy-mm-dd',
        onClose: function( selectedDate ) {
          $( "#end_date" ).datepicker( "option", "minDate", selectedDate );
        }
  });
  $( "#end_date" ).datepicker({
        showButtonPanel: true,
        defaultDate: "+1w",
        numberOfMonths: 2,
        dateFormat:'yy-mm-dd',
        showAnim:"slideDown",
        onClose: function( selectedDate ) {
          $( "#start_date" ).datepicker( "option", "maxDate", selectedDate );
        }
  });
</script>
