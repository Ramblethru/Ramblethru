$("#note-results").html("")
$("#no-results").html("")

<% if @ramble.count > 0 %>
    $("#note-results").append("<%= j render(@ramble) %>");
<% else %>
    $("#no-results").append("Sorry, no results were returned for your search.");
<% end %>